# Configure persistent volume claim
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frontend-pvc
  namespace: default
spec:
  storageClassName: "cloud"
  accessMode:
    -   ReadWriteOnce
  resources:
    requests:
      storage: 5Gi